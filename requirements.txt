# SatQuery AI — serverless API dependencies (read by Vercel's Python runtime)
# Mirrors api/requirements.txt; kept at repo root because the Vercel Python
# builder resolves the dependency manifest from the project root.
fastapi>=0.115.0
uvicorn>=0.32.0
python-multipart>=0.0.12
numpy>=1.26.0,<3
pillow>=10.4.0
tifffile>=2024.8.30
scipy>=1.14.0
